#!/usr/bin/env ruby

require 'logger'

begin
  require File.expand_path(File.join(%w(.. .. lib fidelity)), __FILE__)
  Fidelity::Exec.run(ARGV, Logger.new(STDOUT))
rescue Exception => e
  warn e.message
  exit 1
end
